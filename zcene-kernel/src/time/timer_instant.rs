use core::time::Duration;

////////////////////////////////////////////////////////////////////////////////////////////////////

pub trait TimerInstant {
    fn duration_since(&self, other: &Self) -> Duration;
}
