use crate::time::TimerInstant;

////////////////////////////////////////////////////////////////////////////////////////////////////

pub trait Timer {
    type Instant: TimerInstant;

    fn now(&self) -> Self::Instant;
}
