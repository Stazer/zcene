use ztd::{Display, Error};

////////////////////////////////////////////////////////////////////////////////////////////////////

#[derive(Clone, Debug, Display, Error)]
pub enum ActorCreateError {
    Unknown,
    Custom,
}
